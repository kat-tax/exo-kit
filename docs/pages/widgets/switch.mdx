import 'react-exo/switch.css';
import {Switch} from 'react-exo/switch';
import {Button, Callout} from 'vocs/components';
import {Theme} from '../../utils/theme';

# Switch Widget

> A component that allows selection of a value within a range.

<Theme>
  {theme => (
    <Switch
      disabled={false}
      onValueChange={console.log}
      thumbColor={theme === 'dark' ? '#222' : '#555'}
      onColor={theme === 'dark' ? '#555' : '#CCC'}
      offColor={theme === 'dark' ? '#333' : '#999'}
    />
  )}
</Theme>

```tsx twoslash
import React from 'react';
// ---cut---
// @log: ↓ Import the component
import {Switch} from 'react-exo/switch';

// @log: ↓ Try the example
<Switch
  value={true}
  disabled={false}
  onValueChange={console.log}
  thumbColor="#222"
  onColor="#555"
  offColor="#333"
/>
```

## Technology

| Platform | Library | Support | Notes |
| -------- | ---------- | :--------: | :---- |
| Web | [@radix-ui/react-checkbox](https://www.radix-ui.com/primitives/docs/components/switch) | ✅ | - |
| Native | [React Native Switch](https://reactnative.dev/docs/switch) | ✅ | - |
