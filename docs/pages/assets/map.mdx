import {Map} from 'react-exo/map';
import {Theme} from '../../utils/theme';

# Map

> Displays maps and geo data via [maplibre](https://maplibre.org)

<Theme>
  {theme => (
    <Map
      style={{height: 400}}
      attributionControl={false}
      mapStyle={`https://api.maptiler.com/maps/dataviz-${theme}/style.json?key=UbdBChbHpiVOSIdTJWvV`}
    />
  )}
</Theme>

```tsx twoslash
import React from 'react';
// ---cut---
// @log: ↓ Import the component
import {Map} from 'react-exo/map';

// @log: ↓ Try the example
<Map
  mapStyle="https://api.maptiler.com/maps/dataviz-dark/style.json?key=<maptiler access token>"
  style={{
    height: 400,
    width: '100%',
  }}
/>
```
:::warning
Remember to replace the access token with your own from [Maptiler](https://maptiler.com/)
:::

## Technology

| Platform | Library | Support | Notes |
| -------- | ---------- | :--------: | :---- |
| Web | [maplibre-gl-js](https://github.com/maplibre/maplibre-gl-js) | ✅ | - |
| Native | [maplibre-native](https://github.com/maplibre/maplibre-native) | ✅ | - |
